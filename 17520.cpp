#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

struct number {
	int one;
	int zero;
	int plus;
}number;


int main () {
	int n, index=0,count;//숫자 카운트(-1, 0, 1)
	int numcount; //문자열 갯수
	int a[100001];
	//0이 나오면 1.... 1이 나오면 -1
	cin >> n;
	for(index = 0; index < n; index) {
		if(index<n) {
			a[index] = 0
		}
	}

}

a[index+1] = Add;

//문자열 추가 함수
int Add(count) {
	if(count=0) {
	return number.one= 1;
	return number.zero = -1;
}
else if(count =1 ){
	return 1;
}
else if(count =-1) {
	return 0;
}
